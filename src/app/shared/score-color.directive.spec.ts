import { ScoreColorDirective } from './score-color.directive';

describe('ScoreColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScoreColorDirective();
    expect(directive).toBeTruthy();
  });
});
